spring:
  datasource:
    url: jdbc:h2:mem:voca;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
app:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret-test-secret
    expiration-minutes: 60
